# =============================================
# OpenCode Server Configuration
# =============================================

# GitHub Copilot token for authentication (required)
# How to get it:
#   1. Install opencode locally: curl -fsSL https://opencode.ai/install | bash
#   2. Run: opencode auth login (select GitHub Copilot)
#   3. Copy the token from ~/.config/opencode/auth.json
COPILOT_TOKEN=

# Server password — set this for production deployments!
# All users must enter this password to access the Web UI.
# If empty, the server has no access control.
OPENCODE_SERVER_PASSWORD=

# Port for the OpenCode server (includes built-in Web UI)
OPENCODE_PORT=4096
