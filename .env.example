# =============================================
# OpenCode Server Configuration
# =============================================

# Path to your local opencode auth config directory
# Run `opencode auth login` first, then set this to the config path:
#   Linux/Mac:  ~/.config/opencode
#   Windows:    C:\Users\YourName\.config\opencode
OPENCODE_AUTH_PATH=~/.config/opencode

# Server password — set this for production deployments!
# If empty, the server has no access control.
OPENCODE_SERVER_PASSWORD=

# Port for the OpenCode server (includes built-in Web UI)
OPENCODE_PORT=4096
