version: "3.8"

# Windows override — use this with:
#   docker-compose -f docker-compose.yml -f docker-compose.windows.yml up --build

services:
  opencode-server:
    volumes:
      # Windows-specific path for OpenCode auth config
      - C:\Users\Harry\.config\opencode:/root/.config/opencode:ro
